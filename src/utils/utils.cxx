#include "utils.h"


int splitImage(const cv::Mat& img, size_t patch_size, std::vector<cv::Mat> patches){
    // TODO: 将传入的图片切成多个小图
    size_t imgH = img.rows;
    size_t imgW = img.cols;
    return 0;
}